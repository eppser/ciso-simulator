const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
export function sessionMenu(app,confirm=false){
 const g=app.game,ended=['won','lost'].includes(g.phase);
 return `<div class="eyebrow">${ended?'DAY COMPLETE':app.paused?'MANUALLY PAUSED':'LIVE · SIMULATION CONTINUES'}</div><h2>${confirm?'Restart this day?':'Your shift.'}</h2><p class="session-context">${esc(g.org.name)} <span>${esc(g.clock())} · ${g.trust}/5 trust</span></p>${confirm?`<p class="session-warning">This replaces the current run and its progress. Same organization and difficulty, starting from midnight.</p><button data-action="restart-confirm" class="session-danger">Restart from midnight <span>↻</span></button><button data-action="menu">Keep this run</button>`:`<button data-action="resume" class="session-primary">${ended?'Inspect this game':'Resume game'} <span>▶</span></button><button data-action="leaderboard">Public scoreboard <span>↗</span></button><button data-action="restart">Restart this day <span>↻</span></button><button data-action="menu-organizations">Choose another organization <span>↗</span></button><p class="session-note">${ended?'Your finished run stays available until you start another.':app.paused?'Your manual pause remains in place.':'Menu does not pause the day. Use the pause control before opening Menu if you need a break.'}</p>`}`;
}
